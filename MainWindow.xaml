<Window x:Class="BilirkisiMasaustu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bilirkişi Sicil Arama Sistemi"
        Height="700" Width="1200"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Stil tanımlamaları -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <!-- Mavi Buton Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0D6EFD"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0B5ED7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0A58CA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#6C757D"/>
                                <Setter Property="Foreground" Value="#ADB5BD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Gri Buton Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5C636A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#565E64"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#ADB5BD"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Kırmızı Buton Style (Silme işlemleri için) -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#BD2130"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#ADB5BD"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Küçük Gri Buton Style -->
        <Style x:Key="SmallSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5C636A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#565E64"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#ADB5BD"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Yeşil Buton Style (Başarı işlemleri için) -->
        <Style x:Key="SuccessButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#218838"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E7E34"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#ADB5BD"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sarı Buton Style (Favori işlemleri için) -->
        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFC107"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0A800"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D39E00"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#ADB5BD"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Küçük Mavi Buton Style -->
        <Style x:Key="SmallPrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056B3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004085"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#ADB5BD"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ResultListBoxStyle" TargetType="ListBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>

        <Style x:Key="DetailTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
        </Style>

        <!-- Modern Tab Stili -->
        <Style x:Key="ModernTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                                Background="#E9ECEF"
                                BorderBrush="#DEE2E6"
                                BorderThickness="1,1,1,0"
                                CornerRadius="8,8,0,0"
                                Margin="2,0,2,0"
                                Padding="20,12">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0D6EFD"/>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="White"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#0D6EFD"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#6C757D"/>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#0B5ED7"/>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0D6EFD" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Sol taraf - Kurul Bilgisi ve Değiştir Butonu -->
                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                    <Button x:Name="ChangeKuruluButton"
                            Content="🏛️ Kurulu Değiştir"
                            Click="ChangeKuruluButton_Click"
                            Style="{StaticResource SmallSecondaryButtonStyle}"
                            Margin="0,0,10,0"
                            ToolTip="Bilirkişi kurulunu değiştir">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="🔄 Kurulu Değiştir" Click="ChangeKuruluButton_Click"/>
                                <MenuItem Header="🗑️ Kurul Seçimini Temizle" Click="ClearKuruluSelection_Click"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                    <TextBlock x:Name="KuruluInfoTextBlock"
                               Text="Kurul Seçilmedi"
                               Foreground="#E3F2FD" FontSize="10"
                               Margin="0,5,10,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Orta - Başlık -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="🔍 Bilirkişi Sicil Arama Sistemi"
                              FontSize="24" FontWeight="Bold"
                              Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="KuruluInfoLabel"
                              Text="Masaüstü Uygulaması - 1516 Kayıt"
                              FontSize="12" Foreground="#E3F2FD"
                              HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Sağ taraf - boş (gelecekte başka butonlar eklenebilir) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <!-- Gelecekte başka butonlar buraya eklenebilir -->
                </StackPanel>
            </Grid>
        </Border>

        <!-- Arama Alanı -->
        <Border Grid.Row="1" Background="#F8F9FA" Padding="20,15" BorderBrush="#DEE2E6" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Ad Soyad:" FontWeight="SemiBold" Margin="5,0,5,5"/>
                    <Grid>
                        <TextBox x:Name="SearchTextBox"
                                 Style="{StaticResource SearchTextBoxStyle}"
                                 TextChanged="SearchTextBox_TextChanged"
                                 KeyDown="SearchTextBox_KeyDown"
                                 GotFocus="SearchTextBox_GotFocus"
                                 LostFocus="SearchTextBox_LostFocus"/>
                        <Label x:Name="SearchPlaceholder"
                               Content="Bilirkişi adı girin (örn: UFUK GÜNGÖR)"
                               Foreground="Gray"
                               FontSize="16"
                               Padding="15,8"
                               IsHitTestVisible="False"
                               Visibility="Visible"/>
                    </Grid>
                </StackPanel>

                <Button x:Name="SearchButton" Grid.Column="1"
                        Content="🔍 Sorgula"
                        Click="SearchButton_Click"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="10,25,5,5"
                        VerticalAlignment="Bottom"/>

                <Button x:Name="StatsButton" Grid.Column="2"
                        Content="📊 İstatistik"
                        Click="StatsButton_Click"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="5,25,5,5"
                        VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- Ana İçerik - TabControl -->
        <TabControl Grid.Row="2" Margin="10" Background="White" BorderBrush="#DEE2E6" BorderThickness="1">
            <!-- Arama Sekmesi -->
            <TabItem Header="🔍 Bilirkişi Arama" Style="{StaticResource ModernTabItemStyle}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

            <!-- Sol Panel - Arama Sonuçları -->
            <Border Grid.Column="0" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#E9ECEF" Padding="10" CornerRadius="5,5,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" x:Name="ResultCountLabel"
                                      Text="📋 Arama Sonuçları"
                                      FontWeight="SemiBold" FontSize="14"/>

                            <Button Grid.Column="1" x:Name="AddFavoriteButton"
                                   Content="⭐ Favoriye Ekle"
                                   Click="AddFavoriteButton_Click"
                                   Style="{StaticResource WarningButtonStyle}"
                                   IsEnabled="False"/>
                        </Grid>
                    </Border>

                    <ListBox x:Name="ResultListBox" Grid.Row="1"
                             Style="{StaticResource ResultListBoxStyle}"
                             SelectionChanged="ResultListBox_SelectionChanged"
                             BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- Splitter 1 -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                         Background="#DEE2E6" ResizeBehavior="PreviousAndNext"/>

            <!-- Favori Panel -->
            <Border Grid.Column="2" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FFF3CD" Padding="10" CornerRadius="5,5,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" x:Name="FavoriteCountLabel"
                                      Text="⭐ Favoriler (0)"
                                      FontWeight="SemiBold" FontSize="12"/>

                            <Button Grid.Column="1" x:Name="ClearFavoritesButton"
                                   Content="🗑️"
                                   Click="ClearFavoritesButton_Click"
                                   Style="{StaticResource DangerButtonStyle}"
                                   ToolTip="Tüm favorileri temizle"/>
                        </Grid>
                    </Border>

                    <ListBox x:Name="FavoriteListBox" Grid.Row="1"
                             Style="{StaticResource ResultListBoxStyle}"
                             MouseDoubleClick="FavoriteListBox_MouseDoubleClick"
                             BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- Splitter 2 -->
            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                         Background="#DEE2E6" ResizeBehavior="PreviousAndNext"/>

            <!-- Sağ Panel - Detay Bilgileri -->
            <Border Grid.Column="4" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#E9ECEF" Padding="10" CornerRadius="5,5,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="ℹ️ Detay Bilgiler" 
                                      FontWeight="SemiBold" FontSize="14"/>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button x:Name="CopyButton"
                                       Content="📋 Sicil Kopyala"
                                       Click="CopyButton_Click"
                                       Style="{StaticResource SuccessButtonStyle}"
                                       Margin="0,0,5,0"
                                       IsEnabled="False"/>

                                <Button x:Name="CopyTemplateButton"
                                       Content="📄 Şablon Kopyala"
                                       Click="CopyTemplateButton_Click"
                                       Style="{StaticResource SmallPrimaryButtonStyle}"
                                       IsEnabled="False"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Sicil No Vurgusu -->
                    <Border Grid.Row="1" x:Name="SicilHighlight" 
                           Background="#FFF3CD" BorderBrush="#FFEAA7" BorderThickness="1"
                           Margin="10" Padding="15" CornerRadius="25"
                           Visibility="Collapsed">
                        <TextBlock x:Name="SicilNoLabel" 
                                  Text="Sicil No: " 
                                  FontSize="16" FontWeight="Bold" 
                                  HorizontalAlignment="Center"
                                  Foreground="#856404"/>
                    </Border>

                    <!-- Detay Alanı -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Detay Bilgileri -->
                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="5">
                            <StackPanel x:Name="DetailPanel" Margin="5">
                                <TextBlock Text="👈 Soldan bir bilirkişi seçin"
                                          FontSize="14" Foreground="#6C757D"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="20"/>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Splitter -->
                        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"
                                     Background="#DEE2E6" ResizeBehavior="PreviousAndNext"/>

                        <!-- Şablon Alanı -->
                        <Border Grid.Row="2" BorderBrush="#DEE2E6" BorderThickness="1,1,0,0"
                               Background="#F8F9FA" MinHeight="120" MaxHeight="200">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Background="#E9ECEF" Padding="8">
                                    <TextBlock Text="📄 Talimat Keşif Şablonu"
                                              FontWeight="SemiBold" FontSize="12"/>
                                </Border>

                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                             HorizontalScrollBarVisibility="Disabled" Padding="8">
                                    <TextBox x:Name="TemplateTextBox"
                                            Text="Bilirkişi seçin..."
                                            IsReadOnly="True"
                                            TextWrapping="Wrap"
                                            AcceptsReturn="True"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            FontSize="11"
                                            FontFamily="Segoe UI"
                                            Foreground="#495057"
                                            VerticalScrollBarVisibility="Disabled"
                                            HorizontalScrollBarVisibility="Disabled"/>
                                </ScrollViewer>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
                </Grid>
            </TabItem>

            <!-- Meslek Sekmesi -->
            <TabItem Header="💼 Meslek Bazında" Style="{StaticResource ModernTabItemStyle}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Meslek Listesi -->
                    <Border Grid.Column="0" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#E9ECEF" Padding="10" CornerRadius="5,5,0,0">
                                <TextBlock x:Name="ProfessionCountLabel"
                                          Text="💼 Meslekler (0)"
                                          FontWeight="SemiBold" FontSize="14"/>
                            </Border>

                            <!-- Meslek Arama Alanı -->
                            <Border Grid.Row="1" Background="#F8F9FA" Padding="8" BorderBrush="#DEE2E6" BorderThickness="0,1,0,1">
                                <StackPanel>
                                    <Grid>
                                        <TextBox x:Name="ProfessionSearchTextBox"
                                                FontSize="12" Padding="8,5" Margin="0,0,0,5"
                                                TextChanged="ProfessionSearchTextBox_TextChanged"
                                                GotFocus="ProfessionSearchTextBox_GotFocus"
                                                LostFocus="ProfessionSearchTextBox_LostFocus"/>
                                        <Label x:Name="ProfessionSearchPlaceholder"
                                               Content="Meslek ara..."
                                               Foreground="Gray"
                                               FontSize="12"
                                               Padding="13,3"
                                               Margin="0,0,0,5"
                                               IsHitTestVisible="False"
                                               Visibility="Visible"/>
                                    </Grid>

                                    <!-- İl/İlçe Filtreleme -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="5"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <ComboBox x:Name="IlFilterComboBox" Grid.Column="0"
                                                 FontSize="11" Padding="5,3"
                                                 SelectionChanged="IlFilterComboBox_SelectionChanged"/>

                                        <ComboBox x:Name="IlceFilterComboBox" Grid.Column="2"
                                                 FontSize="11" Padding="5,3"
                                                 SelectionChanged="IlceFilterComboBox_SelectionChanged"
                                                 IsEnabled="False"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <ListBox x:Name="ProfessionListBox" Grid.Row="2"
                                     Style="{StaticResource ResultListBoxStyle}"
                                     SelectionChanged="ProfessionListBox_SelectionChanged"
                                     BorderThickness="0"/>
                        </Grid>
                    </Border>

                    <!-- Splitter 1 -->
                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                                 Background="#DEE2E6" ResizeBehavior="PreviousAndNext"/>

                    <!-- Meslek Bilirkişileri -->
                    <Border Grid.Column="2" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#E9ECEF" Padding="10" CornerRadius="5,5,0,0">
                                <TextBlock x:Name="ProfessionBilirkisiCountLabel"
                                          Text="👥 Bilirkişiler"
                                          FontWeight="SemiBold" FontSize="14"/>
                            </Border>

                            <ListBox x:Name="ProfessionBilirkisiListBox" Grid.Row="1"
                                     Style="{StaticResource ResultListBoxStyle}"
                                     SelectionChanged="ProfessionBilirkisiListBox_SelectionChanged"
                                     MouseDoubleClick="ProfessionBilirkisiListBox_MouseDoubleClick"
                                     BorderThickness="0"/>
                        </Grid>
                    </Border>

                    <!-- Splitter 2 -->
                    <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                                 Background="#DEE2E6" ResizeBehavior="PreviousAndNext"/>

                    <!-- Meslek Detay Bilgileri -->
                    <Border Grid.Column="4" BorderBrush="#DEE2E6" BorderThickness="1" CornerRadius="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#E9ECEF" Padding="10" CornerRadius="5,5,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="ℹ️ Detay Bilgiler"
                                              FontWeight="SemiBold" FontSize="14"/>

                                    <Button x:Name="ProfessionCopyButton" Grid.Column="1"
                                           Content="📋 Sicil Kopyala"
                                           Click="ProfessionCopyButton_Click"
                                           Style="{StaticResource SuccessButtonStyle}"
                                           IsEnabled="False"/>
                                </Grid>
                            </Border>

                            <!-- Sicil No Vurgusu -->
                            <Border Grid.Row="1" x:Name="ProfessionSicilHighlight"
                                   Background="#FFF3CD" BorderBrush="#FFEAA7" BorderThickness="1"
                                   Margin="10" Padding="15" CornerRadius="25"
                                   Visibility="Collapsed">
                                <TextBlock x:Name="ProfessionSicilNoLabel"
                                          Text="Sicil No: "
                                          FontSize="16" FontWeight="Bold"
                                          HorizontalAlignment="Center"
                                          Foreground="#856404"/>
                            </Border>

                            <!-- Detay Alanı -->
                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="5">
                                <StackPanel x:Name="ProfessionDetailPanel" Margin="5">
                                    <TextBlock Text="👈 Soldan bir bilirkişi seçin"
                                              FontSize="14" Foreground="#6C757D"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="20"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusLabel" Grid.Column="0"
                          Text="Hazır - JSON dosyası yükleniyor..." 
                          FontSize="11" Foreground="#6C757D"/>
                
                <TextBlock x:Name="VersionLabel" Grid.Column="1"
                          Text="v1.0.0 - .NET 8" 
                          FontSize="11" Foreground="#6C757D"/>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4"
               Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="10"/>
                <TextBlock Text="Veriler yükleniyor..."
                          Foreground="White" FontSize="16"
                          HorizontalAlignment="Center" Margin="10"/>
            </StackPanel>
        </Border>

        <!-- Bildirim Overlay -->
        <Border x:Name="NotificationOverlay" Grid.RowSpan="4"
               Background="Transparent" Visibility="Collapsed"
               HorizontalAlignment="Right" VerticalAlignment="Top"
               Margin="20">
            <Border Background="#28A745" CornerRadius="5" Padding="15,10"
                   BorderBrush="#1E7E34" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✅" FontSize="16" Margin="0,0,8,0"/>
                    <TextBlock x:Name="NotificationText"
                              Text="Sicil kopyalandı!"
                              Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
